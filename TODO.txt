- add strength detection for password (so user can't enter a weak password)
son we have beter stuf to do
- implement material storage on the server side
- use db for username & password storage
- what happens if user's session token is invalid or expired? backend should tell frontend to wipe it from local storage, right?
- should scryptSync be used if its synchronous and will block the server?